<h1> My Bookings</h1>
    <% if bookings.any? %>
      <ul>
        <% bookings.each do |booking| %>
          <li>
            <%= booking.chef_name %> - <%= booking.date %> at <%= booking.time %>
          </li>
        <% end %>
      </ul>
    <% else %>
      <p>No upcoming bookings.</p>
    <% end %>

<p>
<%= link_to "Create New Booking", bookings_create_path %>
</p>
<p>Find me in app/views/bookings/index.html.erb</p>


<h1>Your Bookings for <%= chef_name %></h1>
    <p>You have <%= user_bookings_count %> <%= pluralize(user_bookings_count, 'booking') %> with <%= chef_name %>.</p>
