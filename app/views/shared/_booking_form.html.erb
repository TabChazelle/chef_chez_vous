test
<div class="booking-form">
<h1>New Booking</h1>

<div class="container py-5">
  <div class="row">
    <div class="col-12 col-md-6 offset-lg-3">
      <div class="bg-white rounded p-3 shadow">
        <h3>Book a Chef</h3>
        <%= simple_form_for @booking do |f| %>
        <%= f.input :start_date, as: :string, input_html: { data: { controller: "flatpickr" }, class: "datepicker" } %>
        <%= f.input :end_date, as: :string, input_html: { data: { controller: "flatpickr" }, class: "datepicker" } %>
        <%= f.input :chef_id, collection: Chef.all, label_method: lambda { |chef| "#{chef.id} - #{chef.name}" } %>
        <%= f.submit class: 'btn btn-flat' %>
        <% end %>
      </div>
    </div>
  </div>
</div>
<%= link_to 'Show My Bookings', bookings_path(@booking) %>
</div>
